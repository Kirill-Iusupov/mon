export enum RoutesUrls {
  root = '/',
  home = '/home',
  employers = '/employers',
  businessTrips = '/businessTrips',
  reports = '/reports',
  login = '/login',
  logout = '/logout',
  settings = '/settings',
  notFound = '/not-found',
  structure = '/structure',
  schedule = '/schedule',
}
